apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: production
  labels:
    io.kompose.service: queue-claim0
  name: queue-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
status: {}
